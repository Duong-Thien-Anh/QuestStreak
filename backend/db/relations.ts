// Relations are inferred from FK naming conventions in Drizzle ORM
// No explicit relations needed for MySQL with planetscale mode
// The schema uses FK columns that reference PKs by naming convention
export {};
